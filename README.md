# Laravel starter

This is a starter project for Laravel. Other than the default composer installed laravel, we have added the following:
- Gitpod

Remember to `composer update` to get Laravel to the latest version before starting.
